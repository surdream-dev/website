node_modules
dist
dist-ssr
.DS_Store
*.log

# Lockfiles are intentionally not committed — install resolves from package.json
package-lock.json
yarn.lock
pnpm-lock.yaml

# Local env files — never commit real values. .env.example is the committed template.
.env
.env.*
!.env.example

.vscode/*
!.vscode/extensions.json
.idea

coverage
*.tsbuildinfo
